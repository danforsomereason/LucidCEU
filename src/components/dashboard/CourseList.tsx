// you will need to get course progress
// for each list item in the course list
// conditionally render button based on amount of course progress

import * as React from 'react';
import Box from '@mui/material/Box';
import List from '@mui/material/List';
import ListItem from '@mui/material/ListItem';
import ListItemButton from '@mui/material/ListItemButton';
import ListItemIcon from '@mui/material/ListItemIcon';
import ListItemText from '@mui/material/ListItemText';


// Method 1
{/* <ListItemIcon>
  {thisCourse.courseProgress === 0 ? <StartIcon> : <ResumeIcon>}
</ListItemIcon>

// Method 2
<ListItemIcon>
  {thisCourse.certificate === true && <RetakeIcon>}
  {thisCourse.courseProgress === 0 && <StartIcon>}
  {thisCourse.courseProgress > 0 && <ResumeIcon>}
</ListItemIcon> */}

export default function CourseList(){

}